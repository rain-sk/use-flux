import { createFlux, useFlux } from './'

describe('createFlux', () => {
  it('is truthy', () => {
    expect(createFlux).toBeTruthy()
  })
})

describe('useFlux', () => {
  it('is truthy', () => {
    expect(useFlux).toBeTruthy()
  })
})
